# Stable apps for the local cluster ("apps" Flux Kustomization).
# Apps under active development belong in Tilt, not here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
