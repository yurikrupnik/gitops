# Platform controllers reconciled by the "infrastructure" Flux Kustomization.
# Add HelmRepository/HelmRelease or plain manifests here (tekton, rook-ceph,
# kube-prometheus-stack, ...) and list them under resources.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
